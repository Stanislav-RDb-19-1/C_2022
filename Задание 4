#include <iostream>
#include <fstream>
using namespace std;

int main(){

    ifstream input ("numbers.txt");
    string number;

    if (input.is_open()){
     int n = 1;
      while (getline(input, number)){
      cout«n«") "«number«endl;
      n++;
}
}
}
