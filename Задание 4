#include <iostream>
#include <fstream>
using namespace std;

int main(){

    ifstream input ("numbers.txt");
    string number;
    if (input.is_open()){
     int num2 = 1;
      while (getline(input, number)){
      cout<<num2<<") "<<number<<endl;
      num2++;
}
}
}
