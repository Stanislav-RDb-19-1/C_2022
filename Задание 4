#include <iostream>
#include <fstream>
using namespace std;

int main(){

    ifstream fail ("numbers.txt");
    string number;
    if (fail.is_open()){
     int num2 = 1;
      while (getline(fail, number)){
      cout<<num2<<") "<<number<<endl;
      num2++;
}
}
}
