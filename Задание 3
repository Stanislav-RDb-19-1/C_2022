#include <iostream>
#include <fstream>
#include <math.h>
using namespace std;

int main(){

    ofstream output ("numbers.txt");

    if (output.is_open()){
       for(int i = 0 ; i <= 20 ; i++)
        output«pow(2, i)«endl;
}

    else cout « "Unable to open output file";

return 0;
}
