#include <iostream>
#include <fstream>
#include <math.h>
using namespace std;

int main(){

    ofstream fail ("numbers.txt");

    if (fail.is_open()){
       for(int num1 = 0 ; num1 <= 20 ; num1++)
        fail<<pow(2, num1)<<endl;
}

    else cout << "Unable to open output file";

return 0;
}
